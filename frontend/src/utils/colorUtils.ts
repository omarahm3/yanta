// TODO: this file is redundant, remove it
/** Compatibility shim: migrate call sites to shared/utils/color then remove. */
export {
	getDefaultProjectColor,
	getProjectAliasColor,
} from "../shared/utils/color";
