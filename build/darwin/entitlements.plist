<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
	  Hardened Runtime entitlements for YANTA (Developer ID distribution).
	  Apple requires the Hardened Runtime (codesign --options runtime) for
	  notarization. These entitlements grant only what a Wails/WebKit app needs.
	-->

	<!-- WebKit (the Wails webview) JITs JavaScript. Without this the renderer
	     crashes under the Hardened Runtime. -->
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>

	<!-- Allow loading the WKWebView / system frameworks at runtime. -->
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
</dict>
</plist>
